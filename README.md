# stathat
Group project by Andrew Boyer, Timsina Shewonth, and Joshua Still

To run, use the install script (or run python3 -m pip install -r requirements.txt) and then run main.py
You will need to provide a URL, and an idea of what data you want to graph and how.
